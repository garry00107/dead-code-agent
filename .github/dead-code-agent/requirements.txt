# Dead Code Removal Agent — Dependencies
# Pinned for reproducible CI builds

httpx>=0.27.0,<1.0          # GitHub API + Slack webhook calls
autoflake>=2.3.0,<3.0       # Python import cleanup (optional but recommended)
pytest>=8.0.0,<9.0          # For smoke test collection checks
